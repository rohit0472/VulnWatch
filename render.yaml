services:
  - type: web
    name: vulnwatch
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app:create_app()" --workers 2 --threads 4 --timeout 120 --bind 0.0.0.0:$PORT